from typing import List


class Solution:
    def minPathSum(self, grid: List[List[int]]) -> int:
        pass

    def get_min_path_index(self, index_row, index_col, max_m, max_n, grid):
        row = 1
        col = 2
        return row, col
